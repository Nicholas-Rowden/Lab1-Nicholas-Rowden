package ChangeMaker;

/**
 * An enum to represent the form of a denomination.
 * An enum is a special "class" that represents a group of constants (unchangeable variables).
 * In this case, the constants are BILL and COIN, representing the form of the money.
 */
public enum Form {
    BILL, COIN
}
